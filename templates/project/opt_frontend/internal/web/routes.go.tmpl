// Code generated by gostack. DO NOT EDIT MANUALLY.
package web

import (
	"github.com/gin-gonic/gin"

	homepage "{{.ModulePath}}/internal/web/pages/home"
	// gostack:imports
)

// Pages is a marker type. Page routes are registered as a side effect of
// building it, so wire keeps a single binding for *gin.Engine (http_v1.New)
// while still being forced to construct this and wire up the page handlers.
type Pages struct{}

func New(
	router *gin.Engine,
	homePage *homepage.Handler,
	// gostack:params
) *Pages {
	r := router.Group("/")
	r.GET("home", homePage.Handle)
	// gostack:routes

	return &Pages{}
}
